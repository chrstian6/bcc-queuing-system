// app/get-ticket/page.tsx
// The get-ticket flow lives on the home page kiosk (TransactionModal).
import { redirect } from "next/navigation";

export default function GetTicketPage() {
  redirect("/");
}
